# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
    # JankyBorders has a built-in detection of already running process,
    # so it won't be run twice on AeroSpace restart
    'exec-and-forget borders active_color=0xff7592bb inactive_color=0xff494d64 width=8.0'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30


# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'


# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = "move-mouse window-lazy-center"
# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 5
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 7
inner.vertical =   7 
outer.left =       7 
outer.bottom =     7 
outer.top =        7 
outer.right =      7 

#[[on-window-detected]]
#if.app-id = 'com.apple.finder'
#run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'Bitwarden'
run = 'layout floating'

#[[on-window-detected]]
#if.app-id = 'com.apple.Notes'
#run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.Preview'
run = 'layout floating'

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# You can uncomment this line to open up terminal with alt + enter shortcut
# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
alt-cmd-ctrl-enter = 'exec-and-forget open -n /Applications/Ghostty.app'
alt-cmd-ctrl-shift-enter = 'exec-and-forget open -n /Applications/Alacritty.app'
alt-cmd-ctrl-b = '''exec-and-forget osascript -e '
tell application "Safari"
    make new document at end of documents
    activate
end tell'
'''
alt-cmd-ctrl-shift-b = 'exec-and-forget open -n /Applications/Zen.app'
alt-cmd-ctrl-shift-f = '''exec-and-forget osascript -e '
tell application "Finder" 
   make new Finder window
end tell'
'''

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-cmd-ctrl-shift-v = 'layout tiles horizontal vertical'
alt-cmd-ctrl-shift-a = 'layout accordion horizontal vertical'

alt-cmd-ctrl-q = 'close'
alt-cmd-ctrl-f = 'layout floating tiling'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-cmd-ctrl-h = 'focus left'
alt-cmd-ctrl-j = 'focus down'
alt-cmd-ctrl-k = 'focus up'
alt-cmd-ctrl-l = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-cmd-ctrl-shift-h = 'move left'
alt-cmd-ctrl-shift-j = 'move down'
alt-cmd-ctrl-shift-k = 'move up'
alt-cmd-ctrl-shift-l = 'move right'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
alt-cmd-ctrl-d = 'resize smart -50'
alt-cmd-ctrl-i = 'resize smart +50'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
#alt-cmd-ctrl-0 = 'workspace 0'
alt-cmd-ctrl-1 = 'workspace 1'
alt-cmd-ctrl-2 = 'workspace 2'
alt-cmd-ctrl-3 = 'workspace 3'
alt-cmd-ctrl-4 = 'workspace 4'
alt-cmd-ctrl-5 = 'workspace 5'
alt-cmd-ctrl-6 = 'workspace 6'
alt-cmd-ctrl-7 = 'workspace 7' #im,portant for brackets and shit -> solution later
alt-cmd-ctrl-8 = 'workspace 8'
alt-cmd-ctrl-9 = 'workspace 9'
alt-cmd-ctrl-c = 'workspace C' #coding? 
alt-cmd-ctrl-t = 'workspace T' # terminals typing mode
alt-cmd-ctrl-p = 'workspace P' #proxmox
alt-cmd-ctrl-g = 'workspace G' # Gaming?
#alt-cmd-ctrl-s = 'workspace S' # ssh 
alt-cmd-ctrl-n = 'workspace N' # notes
#kinda redundant, need better ones

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
#alt-control-command-shift-0 = 'move-node-to-workspace 0'
alt-cmd-ctrl-shift-1 = 'move-node-to-workspace 1'
alt-cmd-ctrl-shift-2 = 'move-node-to-workspace 2'
alt-cmd-ctrl-shift-3 = 'move-node-to-workspace 3'
alt-cmd-ctrl-shift-4 = 'move-node-to-workspace 4'
alt-cmd-ctrl-shift-5 = 'move-node-to-workspace 5'
alt-cmd-ctrl-shift-6 = 'move-node-to-workspace 6'
alt-cmd-ctrl-shift-7 = 'move-node-to-workspace 7'
alt-cmd-ctrl-shift-8 = 'move-node-to-workspace 8'
alt-cmd-ctrl-shift-9 = 'move-node-to-workspace 9'
alt-cmd-ctrl-shift-c = 'move-node-to-workspace C'
alt-cmd-ctrl-shift-t = 'move-node-to-workspace T'
alt-cmd-ctrl-shift-p = 'move-node-to-workspace P'
alt-cmd-ctrl-shift-g = 'move-node-to-workspace G'
#alt-control-command-shift-s = 'move-node-to-workspace S'
alt-cmd-ctrl-shift-n = 'move-node-to-workspace N'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-cmd-ctrl-s = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-cmd-ctrl-w = 'move-workspace-to-monitor --wrap-around next'

alt-cmd-ctrl-up = 'fullscreen'
#alt-down = 'macos-native-minimize'


# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-cmd-ctrl-0 = 'mode moving'
alt-t = 'mode typing'
alt-m = 'mode moving'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
#s = ['layout sticky tiling', 'mode main'] # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']


[mode.typing.binding]
#in case you need to change focus
#alt-left = 'focus left'
#alt-down = 'focus down'
#alt-up = 'focus up'
#alt-right = 'focus right'

alt-t = 'mode main'

[mode.moving.binding]
# for mooooving 

h = ['join-with left', 'mode main']
j = ['join-with down', 'mode main']
k = ['join-with up', 'mode main']
l = ['join-with right', 'mode main']

enter = ['reload-config', 'mode main']
backspace = ['close-all-windows-but-current', 'mode main']
esc = 'mode main'
